{% extends 'base.html.twig' %}

{% block title %}Situation{% endblock %}

{% block body %}
    <h1>Situation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ situation.id }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ situation.texte }}</td>
            </tr>
            <tr>
                <th>Identifiant</th>
                <td>{{ situation.identifiant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_situation_index') }}">back to list</a>

    <a href="{{ path('app_situation_edit', {'id': situation.id}) }}">edit</a>

    {{ include('situation/_delete_form.html.twig') }}
{% endblock %}
