{% extends 'gestion/base.html.twig' %}

{% block title %}Modification Thème{% endblock %}

{% block css %}
    <link rel="stylesheet" href="{{ asset("/css/gestion/form.css") }}"/>
{% endblock %}

{% block gestion %}
    <h1>{{ theme.nom }}</h1>

    {{ include('gestion/theme/_form.html.twig', {'button_label': 'Modifier'}) }}

            <div class="links">
    <a href="{{ path('app_gestion_theme_index') }}">Retour</a>
            </div>

            <div class="links">
                <a href="{{ path('app_theme_delete', {'id': theme.id}) }}">Supprimer</a>
            </div>
{% endblock %}
