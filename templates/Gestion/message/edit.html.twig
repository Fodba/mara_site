{% extends 'gestion/base.html.twig' %}

{% block title %}Modification Thème{% endblock %}

{% block css %}
    <link rel="stylesheet" href="{{ asset("/css/gestion/form.css") }}"/>
{% endblock %}

{% block gestion %}
    <h1>{{ message.identifiant }}</h1>

    {{ include('gestion/message/_form.html.twig', {'button_label': 'Modifier'}) }}

            <div class="links">
    <a href="{{ path('app_gestion_message_index') }}">Retour</a>
            </div>

            <div class="links">
                <a href="{{ path('app_message_delete', {'id': message.id}) }}">Supprimer</a>
            </div>
{% endblock %}
